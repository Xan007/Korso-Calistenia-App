{
  "name": "korso",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile platform for university calisthenics adherence",
  "scripts": {
    "dev": "concurrently \"cd apps/api && npm run dev\" \"cd apps/mobile && npm run start\"",
    "dev:mobile": "cd apps/mobile && npm run start",
    "dev:api": "cd apps/api && npm run dev",
    "android": "cd apps/mobile && npm run android",
    "build": "cd apps/api && npm run build",
    "install:all": "npm install && cd apps/api && npm install && cd ../mobile && npm install && cd ../..",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
